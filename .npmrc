shamefully-hoist=false
strict-peer-dependencies=false

# GitHub Packages registry for @dhilipbinny scope (Kairo Enterprise)
# Token injected at build time via GITHUB_TOKEN env var — never committed
@dhilipbinny:registry=https://npm.pkg.github.com
