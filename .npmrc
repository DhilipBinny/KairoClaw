shamefully-hoist=false
strict-peer-dependencies=false

# GitHub Packages registry for @bsigma-ai scope (Kairo Enterprise)
# Token injected at build time via GITHUB_TOKEN env var — never committed
@bsigma-ai:registry=https://npm.pkg.github.com
